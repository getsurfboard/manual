# 策略组

策略组是 Surfboard 配置文件格式的重要组成部分，允许组织和管理多个代理策略。它们支持复杂的行为，如手动选择、基于延迟的自动切换和负载均衡。

策略组是代理的集合，当前选择其中一个代理作为该组的活动策略。

策略组内的选择可以由用户手动更改，也可以由应用程序根据预定义的标准自动更改。

[[规则 (Rule)]](/docs/profile-format/rule) 的目的地可以设置为策略组或单个代理。当设置为策略组时，流量将通过该组当前选择的代理进行路由。