# 规则

`[Rule]` 部分是 Surfboard 流量管理系统的核心。它定义了如何根据各种标准（如域名、IP 地址和应用程序包名）路由网络流量。

Surfboard 还支持**逻辑规则** (`AND`, `OR`, `NOT`)，用于组合多个标准以进行高级路由。

匹配 `[Rule]` 部分定义的规则的流量将被重定向到指定的代理或策略组。

流量将按照配置文件中定义的顺序，从上到下依次匹配规则。匹配到的第一条规则将决定其去向。