# 域名映射

`[Host]` 部分用于定义静态 DNS 映射，允许用户覆盖特定域名或通配符模式的默认 DNS 解析。

此功能对于本地开发、测试使用自定义域名的服务器或在不修改系统全局 hosts 文件的情况下强制特定 IP 解析特别有用。

你可以将域名映射到单个 IP 地址，也可以映射到多个 IP 地址（用逗号分隔，支持 IPv4 和 IPv6 混合）。配置多个 IP 时，DNS 响应将包含所有匹配的地址，并根据查询类型进行过滤（A 记录仅返回 IPv4，AAAA 记录仅返回 IPv6）。

参考资料：https://zh.wikipedia.org/wiki/域名系统